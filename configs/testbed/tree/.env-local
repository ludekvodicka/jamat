# Classified by the leading `.env`, so this is Text: Raw only, never Rendered.
# Every value here is fake and belongs to nothing.
TESTBED_NAME=file-viewer-testbed
TESTBED_PORT=8080
TESTBED_TOKEN=not-a-real-token-0000
TESTBED_VCS=git
