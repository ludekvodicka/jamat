# {{name}}

{{description}}

A Python 3.11+ project built on the Anthropic SDK.

## Setup

```bash
python -m venv .venv && . .venv/bin/activate
pip install -e .
python -m src.main
```

## Structure

- `src/pipeline.py` - the pipeline this project is about
- `src/main.py` - entry point
- `tests/` - pytest suite
