# {{name}}

{{description}}

A Node.js and Express service written in TypeScript.

## Run

```bash
npm install
npm run dev   # http://localhost:4000
```

## Endpoints

- `GET /health` - liveness probe
- `POST /v1/...` - see `src/routes`
