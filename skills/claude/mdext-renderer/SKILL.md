---
name: mdext-renderer
description: "Author .md or .mdext that Jamat renders richly: GFM, highlighted code, SVG, Mermaid, Graphviz, Vega-Lite, Archify, callouts, status chips, and collapsibles. Use for reports, plans, design notes, and anything the user will view in Jamat. Output stays valid Markdown. Triggers: 'mdext', 'vizuální dokument', 'diagram do reportu'."
user-invocable: true
allowed-tools:
  - Read
  - Write
  - Edit
---

# mdext-renderer

This skill directory is `<checkout>/skills/claude/mdext-renderer` in an AppJamatV3 checkout; when
it is installed through a junction, resolve the real directory first. Read
`<checkout>/mdext-renderer/USAGE.md` completely before authoring, then follow it. Read its linked
reference or examples only when needed. This adapter owns Claude discovery only.
