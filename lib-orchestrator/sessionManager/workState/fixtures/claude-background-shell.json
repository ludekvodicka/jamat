{
  "agent": "claude",
  "expected": {
    "hint": "background"
  },
  "provenance": {
    "origin": "AppJamat (V1) core/agents/claude/fixtures/work-detection.json",
    "case": "background shell",
    "capture": "sanitized capture of a live TUI tail; the V1 corpus is built by capturing raw and rendered tails from that app's diagnostic and stripping prompts, paths and ids (AppJamat docs/architecture/agent-work-detection.md)",
    "ported": "rawTail, screenTail and wideScreenTail copied byte for byte from the V1 case; nothing was written for this tree. The V1 `phase` field is dropped: frames here carry no phase.",
    "identical": "AppJamatV2 core/agents/*/fixtures/work-detection.json holds the same corpus byte for byte",
    "note": "The 2026-08-20 decision made background work green, so this frame's truthful verdict moved from idle to background: its footer says tasks are still running. It is also the frame that admits the shell pattern."
  },
  "frame": {
    "rawTail": "",
    "screenTail": "────────────────────────\n· 1 shell · ctrl+t to hide tasks\n> ",
    "wideScreenTail": "────────────────────────\n· 1 shell · ctrl+t to hide tasks\n> "
  }
}
